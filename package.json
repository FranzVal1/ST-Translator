{
  "name": "sillytavern-safe-translation",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check index.js && node --check parser.js && node --check translation.js && node --check provider.js && node --check runtime.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "jquery": "^3.7.1",
    "jsdom": "^26.1.0"
  }
}
